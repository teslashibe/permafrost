# {{.Name}}

Stub strategy generated by `permafrost strategy new`. Fill in `Decide()`
in `strategy.go` with your logic.

## Configure

`agent create --strategy {{.Name}}` accepts these JSON keys via
`--config-json`:

- (none yet — define them in `parseConfig` and document them here)

## Run

```bash
permafrost agent create --strategy {{.Name}} --perp hyperliquid --alloc 100
permafrost agent run <id>            # foreground iteration
# OR
permafrost agent start <id>          # daemon-supervised
```

## Test

```bash
go test ./{{.Path}}/...
```
